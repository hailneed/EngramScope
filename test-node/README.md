# Node scanner tests

`scanner.test.js` covers supported instruction paths, read-only exclusion of sensitive files, duplicate detection, and home discovery for Claude Code project auto-memory, Codex memories, and Gemini context.
