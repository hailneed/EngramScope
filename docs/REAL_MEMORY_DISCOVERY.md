# Real memory discovery

AgentMemora distinguishes persistent memory files from instructions and rules in the dashboard. Claude Code project auto-memory under `~/.claude/projects/<project>/memory/` and Codex memory files under `$CODEX_HOME/memories/` are treated as `memory`; agent instruction files and Cursor rules remain separately classified.
